{
  "name": "@outreach-engine/compliance-rules",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint . --config ../config/eslint.config.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@outreach-engine/db": "workspace:*",
    "@outreach-engine/types": "workspace:*"
  },
  "devDependencies": {
    "@outreach-engine/config": "workspace:*",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
