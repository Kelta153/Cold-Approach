# fly.toml app configuration file generated for outreach-engine-api on 2026-07-31T20:24:38Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'outreach-engine-api'
primary_region = 'lhr'

[build]
  dockerfile = 'Dockerfile'

[deploy]
  release_command = 'pnpm --filter @outreach-engine/db db:migrate:deploy'

[env]
  NODE_ENV = 'production'
  PORT = '3001'

[http_service]
  internal_port = 3001
  force_https = true
  auto_stop_machines = 'off'
  auto_start_machines = true
  min_machines_running = 1

  [[http_service.checks]]
    interval = '30s'
    timeout = '5s'
    grace_period = '10s'
    method = 'GET'
    path = '/health'
    protocol = 'http'

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 512
